<div dir="rtl" style="text-align: right;">

# طباعة باركود SKU — Order SKU Barcode Printer (`Order-SKU-Barcode-Printer`)

![version](https://img.shields.io/badge/version-v1.2.1-blue)

**بتعمل إيه:** بترجّع بيانات الباركود عشان تتطبع ليبل **٢×١ إنش** لكل صنف —
نص الـ SKU فوق، وتحته باركود `CODE128` بيشفّر **رقم الـ Barcode** المسجّل على
المتغيّر في شوبيفاي. ومن v1.2.0 **بتسجّل الطباعة في D1**.
**مين بيستخدمها:** المخزن — تجهيز **دفعة أوردرات** وإعادة طباعة ليبل ضاع.
**الإصدار:** الـ Worker `v1.2.1` (`WORKER_VERSION` في `index.js`)

```
tool في D1  : order_sku_barcode_printer
type        : print · print_sku        ← جداد في v1.2.0
```

> 🔴 **بند مفتوح إلزامي:** الصف ده **لسه ما اتسجّلش في `ecommoda-constants`
> §7**، والقاعدة (Rule 7) بتقول التسجيل **قبل** أول `writeLog` مش بعده.
> التسجيل بيتعمل في محادثة تحديث مهارات — تحت في «مسائل مفتوحة».

> 🔴 **الريبو ده فيه الـ Worker بس.** الواجهة **مش هنا** — هي صفحة
> `sku-barcode.html` جوّه هب
> [`Warehouse-Operations-Center`](https://github.com/ecommoda-dev/Warehouse-Operations-Center).
> مفيش `index.html` هنا، ومفيش GitHub Pages مطلوبة من الريبو ده.

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Warehouse-Operations-Center/sku-barcode.html
الـ Worker : https://order-sku-barcode-printer-worker.ecommoda-dev.workers.dev
مجموعة السر : warehouse_ops          ← ecommoda-constants §6
tool في D1  : order_sku_barcode_printer   ← print · print_sku (من v1.2.0)
```

## 🔴 الانحراف المعتمد الباقي عن `ecommoda-worker-builder`

الأداة كانت بتكسر **بندين** من Step 2 بقرار صريح (أحمد، 06-09-2026).
**واحد منهم اتقفل في v1.2.0** والتاني لسه ساري.

### ✅ الانحراف ١ — Rule 2 (D1 logging) — **اتقفل في v1.2.0**

الأداة بقت **بتكتب صف سجل** لكل عملية طباعة (قرار أحمد 08-09-2026). السبب
اللي قفل البند هو **نفس الثمن** اللي كان مكتوب هنا وقت اتخاذ القرار الأول:
«مفيش أي أثر لمين طبع إيه وإمتى — لو اتطبع باركود غلط على منتج، مفيش طريقة
تعرف مين ولا إمتى». تاب «سجل العمليات» في `sku-barcode.html` اتضاف عشان ده
بالظبط.

| | الحالة |
|---|---|
| كتابة على شوبيفاي | ❌ **صفر — لسه** |
| كتابة في D1 | ✅ صف سجل append-only لكل عملية طباعة |
| قيمة `tool` | `order_sku_barcode_printer` |
| `type` | `print` (ليبلات أوردر) · `print_sku` (ليبل بدون أوردر) |
| تاب سجل عمليات | ✅ في `sku-barcode.html` |

**شكل الصفوف — والسبب:**

| الحالة | الصفوف | ليه |
|---|---|---|
| دفعة فيها ٣ أوردرات | **٣ صفوف** — صف لكل أوردر | صف واحد للدفعة = `order_name` فاضي، والسجل مش قابل للبحث برقم أوردر — وده **أول** سؤال بيتسأل. وصف لكل ليبل = دفعة ٤٢ ليبل بتكتب ٤٢ صف |
| أصناف بدون أوردر | **صف لكل صنف** | هنا الصنف **هو** وحدة العملية (مفيش أوردر يجمّعه)، فالـ `sku` لازم يبقى في عموده |

- `delta` = **عدد الليبلات** للصف ده — الرقم الوحيد اللي ينفع يتجمّع في تقرير.
- `extra.items` = الأصناف وأعداد نسخها · `extra.batchTotal` = إجمالي الدفعة.
- 🔴 **`batchTotal` بيتحسب في الـ Worker من نفس المصفوفات** — مش بيتقرا من
  حقل بيبعته العميل. رقم من العميل جنب التفاصيل اللي بتناقضه = صف بيكدب
  من غير أي خطأ.
- ⚠️ **كل صفوف الدفعة بنفس `timestamp`** — الدفعة عملية واحدة؛ لو كل صف أخد
  وقته، الصفوف بتتفرّق في الترتيب وتبان كأنها عمليات منفصلة.
- 🔴 **`writeLogBatch` مش حلقة `writeLog`** — التفاصيل في `index.js`
  §SHARED-LOG. `db.batch` بتنفّذ الدفعة **كلها أو ولا حاجة**؛ حلقة عادية
  بتسيب **نص دفعة مسجّلة** لو فشلت في النص.

> 🔴 **بند مفتوح: الصف لسه ما اتسجّلش في `ecommoda-constants` §7.** القاعدة
> (Rule 7) بتقول التسجيل **قبل** أول `writeLog` — والترتيب هنا اتعكس لأن
> `ecommoda-constants` مهارة بتتحدّث في محادثة منفصلة. القيمتان
> (`print` · `print_sku`) **مستقرّتان** ومكتوبتان هنا وفي `CLAUDE.md` بتاع
> الهب، والتسجيل بند مفتوح تحت.

### الانحراف ٢ — Rule 3: «Every tool requires login» — **لسه ساري**

**الأداة بتتطلّب دخول فعلاً — بس نقطة الدخول مش هنا.** مستهلكها الوحيد صفحة
جوّه الهب، وأول سطر في الصفحة `requireSession()`. الدخول بيحصل في
`index.html` بتاع الهب عبر `orders-packing-checker-worker`، وبيتسجّل تحت
`warehouse_ops_center` بـ `appId`.

يعني: **مفيش §SHARED هنا، ومفيش `check_employee`/`register_pin`/
`verify_employee`/`log_logout`** — نسخ الكتلة دي كان هيدّي نقطة دخول تانية
مالهاش لازمة وسر إضافي يتدار.

> ⚠️ **البند ده فضل ساري بعد v1.2.0 كمان — ومعاه نتيجتان:**
> **①** فلتر الموظف في تاب السجل بيتعبّى من **`get_employees` بتاع Worker
> التغليف** (اللي الصفحة بتناديه أصلاً لتحويل تراكينج بوسطة)، مش من هنا.
> **②** `employee` في `log_print` **بييجي من العميل** — الـ Worker بيتحقق من
> السر مش من هوية الموظف. ده **نفس وضع كل أدوات الستاك النهاردة**
> (`Warehouse-Operations-Center/CLAUDE.md` §الاستثناء ٢): المحاسبة على
> الموظف **شرف مش إثبات**. لو اتطلب تتحوّل لإثبات، الحل session token موقّع
> — بند منفصل مكانه `ecommoda-worker-builder`، ومايتحلّش هنا لوحده.

> ⚠️ لو اتعمل للأداة دي **واجهة مستقلة** يومًا ما، البند ده بيرجع ساري
> بالكامل: الكتلة بتتنسخ حرفيًا من `references/shared-functions.md` والـ
> endpoints من `references/auth-endpoints.md`.

## 🔴 مجموعة السر `warehouse_ops` — العضو الرابع

`WORKER_SECRET` هنا **مش قيمة فريدة** — هو **نفس** قيمة الطباعة والتغليف
وحذف المنتج (`ecommoda-constants` §6).

| العضو | النوع |
|---|---|
| `order-printer-worker` | Worker |
| `orders-packing-checker-worker` | Worker |
| `order-item-remover-worker` | Worker |
| **`order-sku-barcode-printer-worker`** | **Worker (انضم 06-09-2026)** |
| `Warehouse-Operations-Center` | واجهة (هب) — `warehouse_ops_worker_secret` |

**السبب:** الهب شاشة إعدادات واحدة بحقل سر واحد. حقل لكل Worker معناه إن
الموظف هيلزق نفس القيمة أربع مرات — نفس النتيجة الأمنية بتكلفة تشغيلية أعلى.

> 🔴 **ده معناه إن `1.html` القديم بيقف.** النموذج الأولي كان بيقرا
> `order_sku_barcode_printer_worker_secret` وبسر خاص بيه؛ أول ما السر يتحوّل
> لقيمة المجموعة، الملف ده بيرجّع `401`. ده **متوقّع ومقصود** (قاعدة ٣ في
> §6): الملف نموذج أولي متجمّد، والواجهة الحقيقية في الهب.

> ⚠️ **التدوير بيمسّ الأربعة + الهب.** الإجراء الكامل في
> `ecommoda-constants` §6 — والبصمة القصيرة في `?action=diag` هي اللي بتثبت
> إن الأربعة على نفس القيمة (**الطول لوحده مش كافي**).

## الـ Endpoints

| الـ action | بيعمل إيه | الميثود |
|---|---|---|
| `get_order` | بنود الأوردر الفعّالة (SKU · barcode · كمية · صورة) + `orderId`. بـ **`order=`** (الاسم) أو **`id=`** (الـ ID الرقمي — v1.1.0) | GET |
| `search_sku` | بحث في متغيّرات المنتجات بـ SKU أو رقم باركود — **من غير أوردر**. ✅ **رجع ليه مستهلك في v1.2.0** · 🔴 **الاستعلام اتصلح في v1.2.1** (تحت) | GET |
| **`log_print`** | **يسجّل دفعة طباعة في D1** — صف لكل أوردر، وصف لكل صنف بدون أوردر (v1.2.0) | **POST** |
| **`get_logs`** | صفحة من السجل (١٠٠ صف · فلترة server-side) | GET |
| **`get_logs_count`** | العدّ الحقيقي بنفس الفلاتر — للتصفيح | GET |
| **`get_logs_export`** | التصدير لحد `LOG_EXPORT_MAX` + `cap`/`total`/`truncated` | GET |
| `get_config` | `version` — الهب بيقارنه بالحد الأدنى (`barcode.min`) | GET |
| `diag` | فحص ذاتي: المتغيّرات · بصمة السر · **D1** · OAuth · الصلاحيات | GET |

كلهم بيتطلّبوا `Authorization: Bearer <WORKER_SECRET>`، و`OPTIONS` بيرجّع 204.

### 🔴 `log_print` — POST مش GET، وحارس `env.DB` قبله

```jsonc
// POST ?action=log_print
{ "employee": "Ahmed_Ibraheem",
  "orders":   [ { "orderId": "63494…", "orderName": "#53768", "source": "bosta",
                  "items": [ { "sku": "FL-PO-10 / Black / 43", "barcode": "34271298", "copies": 2 } ] } ],
  "skuItems": [ { "sku": "RN-AD-115 / Black / 45", "barcode": "41202242", "title": "Adidas Terrex", "copies": 1 } ] }
// → { ok: true, rows: 2 }
```

- 🔴 **POST إلزامي.** الكتابة عمرها ما بتكون على GET — أي prefetch أو إعادة
  تحميل للرابط كان بيكتب صف تاني.
- 🔴 **حارس `env.DB` قبل أي مسار سجل.** من غيره الاستعلام بيرمي
  `Cannot read properties of undefined` — رسالة مالهاش أي علاقة بالسبب
  (binding ناقص أو **Promote ما اتعملش**). نفس منطق `assertEnv`.
- ⚠️ **صنف بـ `copies = 0` مابياخدش صف** — وأوردر كل أصنافه صفر مابياخدش صف
  خالص. الصف بيقول «اتطبع»، وصفر ليبل يعني ما اتطبعش.
- ⚠️ حراس حجم (`LOG_MAX_ORDERS = 100` · `LOG_MAX_SKUS = 200`) بيرجّعوا **400
  صريحة** مش قص صامت.

### 🔴 `search_sku` — الاستعلام كان بيرجّع صفر على SKU موجود (اتصلح في v1.2.1)

الاستعلام لحد `1.2.0` كان بيبعت مدخل الموظف لشوبيفاي **زي ما هو**:

```js
const q = `(sku:${safe}*) OR (barcode:${safe})`;      // ❌ لحد 1.2.0
```

والـ SKU عندنا فيه **مسافات وشرطات مايلة** (`SD1 / Light grey / 45`). بحث
شوبيفاي بيقسّم النص ده لكلمات منفصلة، فـ `Light` و`grey` بقوا **شروط بحث
عامة** والنتيجة **صفر** على صنف **موجود فعلاً**.

> 🔴 **والفشل صامت بالكامل:** الرد `200` بمصفوفة `variants` فاضية — مفيش
> خطأ في الكونسول ولا في الـ Worker. الواجهة بتقول «مفيش أي صنف بالـ SKU
> ده على شوبيفاي» على صنف **الموظف شايفه في جدول الدفعة فوق**.

الحل في `buildSkuQuery`: **كل كلمة في المدخل بتبقى شرط `sku:` بادئة لوحدها،
والشروط بـ `AND`.**

```js
// SD1 / Light grey / 45  →
(sku:SD1*) AND (sku:Light*) AND (sku:grey*) AND (sku:45*)
```

| المدخل | الاستعلام | النتيجة الحيّة (08-09-2026) |
|---|---|---|
| `SD1 / Light grey / 45` | أربع شروط بـ AND | **صنف واحد** بالظبط |
| `SD1 / Light` (جزئي) | شرطين بـ AND | **٤ مقاسات** — بيخدم القايمة المنسدلة |
| `RN-AD` (كلمة واحدة) | `(sku:RN-AD*)` | كل الموديلات اللي بتبدأ بيه |
| `03028802` (رقم صافي) | `(sku:…*) OR (barcode:…)` | الصنف بالباركود |

- 🔴 **`sku:"..."` (اقتباس) مش الحل.** اتجرّب حيًا: بيشتغل للتطابق التام
  بس، و`sku:"SD1 / Light"` بيرجّع **صفر** — يعني كان هيكسر البحث الجزئي
  اللي القايمة المنسدلة في الهب قايمة عليه بالكامل.
- ⚠️ **`barcode:` بيتضاف بس لما المدخل كله رقم صافي** — الباركود رقم مالوش
  مسافات، وخلطه في استعلام فيه `AND` كان بيخلّي أسبقية `AND`/`OR` غامضة
  والنتيجة تفرق من مدخل للتاني.
- ⚠️ **النجمة على كل كلمة مقصودة** — المقاس آخر كلمة في الـ SKU، ومن غير
  النجمة الكلمة الناقصة **وانت بتكتب** مابتطابقش حاجة، والقايمة المنسدلة
  بتفضل فاضية لحد آخر حرف.
- ⚠️ **مدخل كله رموز بيرجّع فاضي من غير نداء** — بدل استعلام مشوّه.

### 🔴 `diag` بقى بيفحص D1 — والفحص بيعدّ صفوف فعلاً

وجود `env.DB` لوحده **مش كفاية**: binding موجود على قاعدة غلط بيرمي عند أول
استعلام مش عند الربط، وتاب السجل ساعتها بيبان **فاضي من غير أي سبب مكتوب**.
عشان كده بند `D1 (DB)` بينفّذ `getLogsCount` فعلاً وبيعرض العدد.

### 🔴 `get_order` بياخد `order=` **أو** `id=` (v1.1.0)

**ده اللي بيخلّي ماسح باركود شوبيفاي في الهب يشتغل.** باركود الأوردر
المطبوع بيشفّر الـ **ID الرقمي** (١٠ خانات فأكتر)، **مش** اسم الأوردر
(٥ خانات) — فبحث `name:` عليه بيرجّع 404 على أوردر **موجود فعلاً**. نفس
التفرقة الموجودة في `orders-packing-checker-worker` من زمان
(`/^\d{10,}$/` → `id`).

| المسار | الاستعلام | ليه |
|---|---|---|
| `order=` | `orders(first:1, query:"name:#X")` | فلتر نصي — الاسم |
| `id=` | **الجذر `order(id: "gid://shopify/Order/X")`** | بيرجّع `null` **صريحة** لو مش موجود |

- 🔴 **الجذر `order(id:)` مش فلتر `id:` جوّه `orders(query:)`** — الفلتر
  النصي بيرجّع **صفر نتايج بلا أي خطأ** لو الشكل مش مظبوط (نفس عيلة الفخ في
  `shopify-graphql-helper` Step 3)، والجذر بيفرّق بين «مش موجود» و«الاستعلام
  غلط».
- 🔴 **`id` بيتفحص إنه أرقام بس (`/^\d+$/`) قبل ما يتركّب في الـ GID** —
  من غير الفحص ده أي نص بيتحقن في معرّف شوبيفاي.
- 🔴 **الحقول معرّفة مرة واحدة في `ORDER_FIELDS`** والاستعلامان بيحقنوها،
  و**`shapeOrder` هي التشكيل الوحيد**. المسارين لازم يرجّعوا نفس الشكل
  بالحرف؛ نسختين من قايمة الحقول كانتا هيفترقا مع أول حقل جديد، والفرق
  بيظهر كعمود فاضي في مسار واحد بس (درس R1).
- ⚠️ **الافتراضي ما اتغيّرش** — نداء بـ `order=` لوحده سلوكه حرفيًا زي
  v1.0.0. الإضافة **مسار جديد** مش تعديل في القديم.

### `get_order` — شكل الرد

```json
{
  "ok": true,
  "orderId": "6349...", "orderName": "#53768",
  "financialStatus": "PENDING", "fulfillmentStatus": "UNFULFILLED",
  "items": [
    { "title": "...", "sku": "FL-PO-10 / Black / 43", "barcode": "34271298",
      "quantity": 1, "variantTitle": "43", "variantId": "5592...", "image": "https://..." }
  ],
  "itemsTruncated": false, "itemsCap": 100
}
```

> ⚠️ **`id=` له الأولوية لو الاتنين اتبعتوا.** الواجهة بتبعت واحد بس،
> والأولوية دي بتمنع رد غامض لو حد نادى المسار بإيده بالاتنين.

### `search_sku` — شكل الرد

```json
{ "ok": true, "term": "FL-PO-10",
  "variants": [ { "variantId": "...", "sku": "...", "barcode": "...",
                  "title": "...", "variantTitle": "43",
                  "productStatus": "ACTIVE", "image": "https://..." } ],
  "truncated": false, "cap": 50 }
```

## 🔴 الفخاخ — اللي لازم يتقرا قبل أي تعديل

### ① الباركود بيشفّر `barcode` مش `sku`

النص اللي فوق الليبل هو **الـ SKU** (`FL-PO-10 / Black / 43`)، والرقم اللي
جوّه الخطوط هو **الـ Barcode** (`34271298`). **الاتنين مختلفين تمامًا**،
وعكسهم بيخلّي السكانر يقرا رقم مالوش وجود في المخزون — **من غير أي خطأ ظاهر**.

اتأكّد حيًا بمقارنة أوردر `#53768` بتطبيق «Retail Force Barcode» الحالي
(06-09-2026).

### ② `sku:` و`barcode:` صالحين على `productVariants` بس

```
✅ productVariants(query: "sku:FL-PO-10*")     ← شغّال، متأكَّد حيًا
✅ productVariants(query: "barcode:34271298")  ← شغّال، متأكَّد حيًا
❌ orders(query: "sku:...")                    ← بيرجّع صفر نتايج **بلا أي خطأ**
```

الفلتران دول ممنوعين على `orders` (`shopify-graphql-helper` Step 3) — والفشل
**صامت**، يعني الأداة تبان شغّالة وهي بترجّع فاضي.

> ⚠️ **النجمة في `sku:${term}*` مقصودة** — الـ SKU عندنا بيشمل اللون والمقاس
> (`FL-PO-10 / Black / 43`)، فبحث بالجزء الأول من غير نجمة بيرجّع صفر.

### ③ `read_products` صلاحية إلزامية — مش `read_orders` بس

رقم الـ Barcode جاي من **المتغيّر**، و`search_sku` بيستعلم `productVariants`
مباشرة. من غير `read_products` الاتنين بيفشلوا بـ top-level error.
`?action=diag` بيفحص الصلاحيتين بالاسم.

### ④ `currentQuantity` مش `quantity`

التانية بتشمل البنود **الملغية** — يعني ليبل بيتطبع لصنف اتشال من الأوردر.

### ⑤ صورة المنتج fallback إلزامي

`ProductVariant.image` مهجورة في `2026-01`، والبديل `media`. **والأهم:** أغلب
متغيّرات المتجر `media.nodes` بترجع `[]` (اتأكّد حيًا على
`FL-PO-10 / Black / 43`) — فمن غير الرجوع لـ `product.featuredMedia` عمود
الصورة بيفضل **فاضي دايمًا**. ده بالظبط اللي كان بيحصل في النموذج الأولي.

### ⑥ القطع بيتبلّغ عنه — مايعدّيش في السكوت

`ORDER_LINE_ITEMS_MAX = 100` و`SKU_SEARCH_MAX = 50`. لما السقف يتضرب، الرد
بيشيل `itemsTruncated` / `truncated` والواجهة بتعرض بانر أصفر. **ليبل ناقص في
السكوت = قطعة بتخرج من المخزن من غير باركود** والموظف فاكر إنه طبع كل حاجة.

## `wrangler.toml` — اللي مطلوب

```toml
name = "order-sku-barcode-printer-worker"   # لازم يطابق الداشبورد بالحرف

[[d1_databases]]                            # 🔴 اتضاف في v1.2.0
binding       = "DB"                        # ⚠️ "DB" بالحرف — أي اسم تاني = كتابة فاشلة في صمت
database_name = "ecommoda-dev-logs"
database_id   = "90db62d3-bd7e-4d92-912b-10fc78eeb565"

[vars]
SHOP_DOMAIN = "6c7e1a-53.myshopify.com"
```

**الأسرار (الداشبورد + Promote، مش في الريبو):**
`WORKER_SECRET` (قيمة `warehouse_ops`) · `CLIENT_ID` · `CLIENT_SECRET`.

## 🔴 CORS — لسه Option A (wildcard)، واتراجع في v1.2.0

قاعدة الاختيار في `references/cors-patterns.md` بتفرّق بين **أدوات كتابة**
(Option B · allowlist) و**أدوات قراءة/عرض** (Option A · wildcard). الأداة
بقت **بتكتب في D1** من v1.2.0، فالبند اتراجع — والقرار إنه **يفضل Option A**:

- الكتابة الوحيدة **صف سجل append-only تحت اسم الأداة نفسها**: صفر لمسة على
  شوبيفاي، وصفر تعديل على أي بيانات قائمة.
- الحماية الحقيقية — قبل التعديل وبعده — هي `WORKER_SECRET` وبس.
- ⛔ **لو الأداة كتبت يومًا حاجة على شوبيفاي، البند ده يترجع فيه فورًا**
  لـ Option B (`ALLOWED_ORIGINS`) في **نفس** التسليم.

## النشر — الحالة الحالية

> 🔴 **الـ Worker لسه مرفوع يدوي من الداشبورد (نسخة النموذج الأولي).**
> الريبو ده لسه **مش مربوط بـ Cloudflare Workers Builds**. الخطوات المطلوبة
> مرة واحدة:
>
> ① Cloudflare → الـ Worker → Settings → Build → **Connect to Git** →
>    `ecommoda-dev/Order-SKU-Barcode-Printer` · فرع `main` · Root `/`
> ② أول `git push` بيعمل build وينشر `index.js` بدل النسخة اليدوية
> ③ حدّث `WORKER_SECRET` لقيمة مجموعة `warehouse_ops` → **Promote**
> ④ تأكد إن **عدد الـ Workers ما زادش** — اختلاف الاسم بيعمل Worker شبح
> ⑤ `?action=diag` من الهب: البصمة لازم تطابق باقي المجموعة، و`read_products` ✅

> ⚠️ **ممنوع لصق الكود في Quick Editor بعد الربط** — أول push بيدهسه والريبو
> يبقى مش مطابق للمنشور. الإجراء الكامل والفخاخ الستة →
> `ecommoda-tool-migration-playbook`.

## مسائل مفتوحة

- 🔴 **ربط Workers Builds لسه ما اتعملش** — النسخة المنشورة دلوقتي هي كود
  النموذج الأولي اليدوي (بلا `get_config` وبلا `search_sku`)، فالهب هيعرض
  «⚠️ Worker باركود SKU نسخة قديمة» لحد ما يتنشر من الريبو.
- 🔴 **`order_sku_barcode_printer` لسه ما اتسجّلش في `ecommoda-constants` §7.**
  القيمتان `print` و`print_sku` **مستقرّتان** ومكتوبتان هنا وفي `CLAUDE.md`
  بتاع الهب، بس Rule 7 بتقول التسجيل **قبل** أول `writeLog` مش بعده —
  والترتيب اتعكس هنا لأن `ecommoda-constants` مهارة بتتحدّث في محادثة
  منفصلة. **مطلوب تحديث مهارة قبل أول تشغيل حي**، وإلا الصفوف بتدخل D1 تحت
  قيمة غير مسجّلة (نفس اللي حصل مع `AWB`/`not_found` في أداة الطباعة).
- 🔴 **`barcode.min` في الهب بقى `1.2.1`** (هب v1.16.0). يعني نشر v1.2.1
  **شرط** مش تحسين:
  - **البحث بالـ SKU الكامل بيرجّع صفر** على `1.2.0` وأقدم (تفاصيل الباج
    فوق §`search_sku`) — و`sku-barcode.html` بقى فيها **قايمة اقتراحات
    بتفتح وانت بتكتب**، فالقايمة بتفضل فاضية على صنف موجود، والرسالة
    بتقول «مفيش أي صنف بالـ SKU ده على شوبيفاي» — جملة **صح شكلاً وغلط
    فعلاً**.
  - **تاب «سجل العمليات» بيرجّع «action غير معروف»** على كل تحديث —
    `get_logs`/`get_logs_count`/`get_logs_export` مش موجودين في 1.1.0.
  - **الطباعة بتشتغل من غير أي أثر في D1** — `log_print` بيرجّع 400،
    والصفحة بتطلّع توست أصفر والورق بيطلع (الفشل مايمنعش الطباعة بقرار).
  - **مربع «طباعة بالـ SKU»** بينادي مسار موجود من 1.0.0، بس **نتيجته
    غلط** قبل 1.2.1 على أي SKU فيه مسافة — يعني التدهور مش «جزئي» في
    الحتة دي، هو **رد فاضي بيتقري إجابة**.
  - و`get_order?id=` (اللي `1.1.0` اتعملت عشانه) لسه شرط: من غيره **ماسح
    باركود شوبيفاي بيفشل على كل مسح** بـ 400 «رقم الأوردر مطلوب».
  التدهور كله **مُعلَن بحارس النسخة** مش صامت.
- 🔴 **`[[d1_databases]]` الجديد محتاج نشر — مش Promote بس.** الـ binding
  بيتقرا من `wrangler.toml` (Workers Builds)، فمن غير الـ build الجديد
  `env.DB` بيفضل `undefined` — وساعتها **الحارس بيرد 500 برسالة صريحة**
  («binding قاعدة البيانات ناقص») بدل `Cannot read properties of undefined`.
  ⚠️ و`?action=diag` بند `D1 (DB)` هو اللي بيثبت إن الربط تمّ فعلاً — بيعدّ
  صفوف الأداة، فـ binding على قاعدة غلط بيبان ❌ مش ✅.
- ✅ **`search_sku` رجع ليه مستهلك (v1.2.0)** — البند اللي كان مفتوح («يتشال
  ولا يفضل؟») **اتقفل بالإبقاء**: مربع «طباعة بالـ SKU» في هب v1.14.0 بينادي
  المسار ده بالـ SKU الكامل. **الشيل بقى تغيير كاسر، مش تنضيف.**
- 🟡 **تراكينج بوسطة بيتحوّل في Worker التغليف مش هنا** — الهب بينادي
  `orders-packing-checker-worker` بـ `get_order?tracking=` وياخد **اسم
  الأوردر** بس، وبعدين بيكمّل هنا. القرار مقصود: §BOSTA موجود في ريبوهين
  بالفعل، ونسخة تالتة هنا كانت هتخلّيهم **تلاتة** يفترقوا مع أول إصلاح في
  شكل رد بوسطة. **متضفش §BOSTA هنا** من غير ما تحل التكرار القايم الأول.
- 🔴 **`WORKER_SECRET` لسه بقيمته الخاصة** — لحد ما يتحوّل لقيمة
  `warehouse_ops` + Promote، صفحة الهب هترجّع `401`.
- 🟡 **`1.html` نموذج أولي متجمّد** — متساب للرجوع التاريخي وبس. **ممنوع
  يتعدّل**: هو بيقرا مفتاح سر قديم، وهيبطل يشتغل بعد انضمام الأداة للمجموعة.
  الواجهة الحقيقية `sku-barcode.html` في الهب.
- 🟢 **مفيش pagination على بنود الأوردر** — سقف ١٠٠ بند وبانر عند القطع.
  أوردر أكبر من كده ما حصلش في المتجر لحد دلوقتي.
- 🟢 **`employee` بييجي من العميل في `log_print`** — نفس وضع كل أدوات الستاك.
  التحويل لإثبات = session token موقّع، بند مكانه `ecommoda-worker-builder`.

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v2.1.0 |
| ecommoda-constants | v1.10.0 |
| shopify-graphql-helper | v1.1.0 |
| ecommoda-tool-migration-playbook | §13 (Promote) · §الربط بـ Builds |

آخر مطابقة: 08-09-2026 · `index.js` v1.2.1
🔴 معلّقة: تسجيل `order_sku_barcode_printer` في `ecommoda-constants` §7

---

آخر تحديث: 08-09-2026 — v1.2.1

</div>
